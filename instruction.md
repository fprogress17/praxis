create git and commit if no git repo

Create the workLog.md if not here
 whenever code change or fix, then add date, time, commit and brief explanation, cause , fix , and agent name(who you are) in latest top to oldest bottom